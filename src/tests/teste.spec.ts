describe("Testa funçoes de uma calculadora", () => { //Describe: agrupa os testes
     it("Faz uma soma", () => {
        const sum = 1 + 1;

        expect(sum).toBe(2)
     }) //it/test faz o teste acontecer
})